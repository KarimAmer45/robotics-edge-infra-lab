---
- name: Install common packages
  ansible.builtin.apt:
    name: "{{ common_packages }}"
    update_cache: true
    state: present

- name: Set timezone
  community.general.timezone:
    name: "{{ timezone }}"